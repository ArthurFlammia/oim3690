# Week 1 Log

## What I worked on this week
Session 01 (5/18) was setup and building my first website. I got my development
environment working (VS Code, Git, GitHub, GitHub Desktop, Chrome), got my personal
site live at https://arthurflammia.github.io, and created my `oim3690` course repo.
Session 02 (5/20) covered how the web actually works — DNS, HTTP, what happens
between typing a URL and a page showing up — plus DevTools and the start of HTML.

## Code I want to highlight
The hand-written HTML skeleton I started `hello.html` with, before enhancing it:

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <title>Hello!</title>
</head>
<body>
  <h1>Hello, world!</h1>
</body>
</html>
```

This is the minimum structure every HTML page needs: a doctype line, the `<html>`
wrapper, a `<head>` for page info like the title, and a `<body>` for the content
you actually see.

## AI usage this week
This one was scripted for us. I wrote 2–3 sentences describing myself and dropped
them into the prompt template from the slides: a single `index.html`, no
frameworks, clean layout, header, short intro, and an interests section. What came
back is basically what's still live — Fraunces and Karla pulled from Google Fonts,
a flex nav bar, and `:root` CSS variables holding the colors, including the
`--green: #0a6b4d` accent.

The `:root` block surprised me. I didn't ask for variables and I'd never seen that
syntax — it looked like the file was declaring colors somewhere separate from where
they got used, and it took me a minute to figure out that `var(--green)` was
pointing back up to it. The Google Fonts import was the other one; I assumed fonts
were just whatever the computer had.

Step 5 that day was to go back and iterate, so I adjusted the accent color and
reworked the interests section after the first version came back. The thing I had
to look up on my own was what `<head>` is actually for, since nothing in it renders.

## One question I still have
Why does the site take a minute to show up after I push? I expected the push and
the live page to be the same event, and they aren't.

### "I Don't Understand" list (from the Session 02 breakout, 5/20)
- What a server actually is — hardware, software, or a role something plays.
- Why my DevTools edits disappear on refresh, when they look identical to real edits.
- The difference between what `nslookup` shows me and what `tracert` shows me.
